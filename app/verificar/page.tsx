export default function TestPage() {
  return (
    <div style={{ padding: '50px', textAlign: 'center', fontSize: '30px' }}>
      <h1>🚀 SI VES ESTO, EL CÓDIGO SE ESTÁ ACTUALIZANDO CORRECTAMENTE</h1>
      <p>Versión: 2.0 - Hora: {new Date().toLocaleTimeString()}</p>
    </div>
  );
}
